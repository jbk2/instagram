<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="jumbotron">
  <div class="container">
    <h2>Hello Instagrammers</h2>
    <p>This is an Instagram clone site built by James B Kemp as a part of his Makers Academy Ruby stack immersive web development course. Within this site we are using many technologies such as; Ruby, Ruby on Rails, Javascript, JQuery, Ruby gems(ImageMagick, Paperclip, AWS-SDK, devise, bootstrap), Rspec, Capybara, HTML5, CSS3, Amazon S3. 
    </p>

    <% if !user_signed_in? %>
      <p><%= link_to('Sign up', new_user_registration_path , class: "btn btn-primary btn-lg")%></p>
    <% end %>
  </div>
</div>

<div class='container'>
  <% if @posts.any? %>
    <div class='row'>
      <%= render @posts %>
    </div>
  <% else %>
    <h3>No posts yet.</h1>
  <% end %>
</div>
